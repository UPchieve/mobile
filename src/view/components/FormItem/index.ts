import { Item } from 'native-base';
import styled from 'styled-components';

export default styled(Item)`
	margin-bottom: 20px;
	margin-left: 0;
	width: 96%;
	height: 60px;
	align-self: center;
`;
