import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	image: {
		width: '80%',
		height: null,
		aspectRatio: 2.34,
		resizeMode: 'contain',
	},
});

export default styles;
